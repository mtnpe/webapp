@using WebApp.Models.BL.InputSlipDetailBL
@model InputSlipDetailDTO
@{
    ViewData["Title"] = "Delete InputSlipDetail";
}

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <!-- SELECT2 EXAMPLE -->
        <div asp-action="DeleteInputSlipDetail" class="card card-default">
            <form asp-action="DeleteInputSlipDetail">
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="Id" class="control-label">Id</label>
                                <input asp-for="Id" class="form-control" readonly />
                            </div>
                            <!-- /.form-group -->
                        </div>

                    </div>
                </div>
                <div class="card-footer">
                    <a asp-action="IndexInputSlipDetail" class="btn btn-secondary">Cancel</a>
                    <input type="submit" value="Delete InputSlipDetail" class="btn btn-success float-right" />
                </div>
            </form>
        </div>
    </div>
    <!-- /.container-fluid -->
</section>
<!-- /.content -->